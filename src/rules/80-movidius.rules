# Installed by `lite_record survive_reboot`. An OAK-D enumerates as a Movidius
# device (03e7) before depthai loads its firmware, and depthai reports
# "Insufficient permissions" unless it can open that node.
SUBSYSTEM=="usb", ATTRS{idVendor}=="03e7", MODE="0666"
